1. python字符编码问题
   数据集的文件编码应该是gbk编码，其中query部分为半角，title部分为全角
   进行处理时，先将gbk字符串decode成unicode字符串，并且把title的全角转化为半角
   在Unicode和半角的情况下计算特征
2. 特征总结
   长度类特征：分词前query和title的长度，长度差的绝对值；分词后词的个数，个数差
               相似度类特征：分词前基于字符串的相似度，编辑距离、最长公共子串，最长公共子序列。。。；分词后，tfidf的余弦相似度，lsi相似度，LDA相似度，Jaccard相似度。。。；
3. lsi和lda的使用（很有用，不过要用对）；
   gensim的使用；官方文档推荐用tfidf
   训练数据的获取很关键
corpus来训练lsi模型；lda用普通的corpus就可以。
4. 分别提取关键词后的特征
5. word2vec有用，就看怎么用了.
   word2vec结果的好坏取决于训练数据的规模和分词的效果.
   分词的效果很关键，对于不同的应用，分词算法都应该有不同之处。
6. badcase分析
   网址类
   语义类
       一义多词：“卫生间”和“厕所”；音译不同形；“描写”和“形容”；“孩子”和“儿童”；“zhangziqian”和“张子谦”；“1001p”和“一千零一页”；英文大小写(可预处理解决)；
一词多义：“qq”
   title去除子标题再计算特征
   其他待分析的

   解决方法：加入规则或者新特征
   
   已解决：
   英文大小写问题，预处理统一为小写，结果降低约0.009；
   title去除子标题再计算特征，融合原始title的特征降低约0.01；
   textrank取top3关键词，计算jaccard和cosine，降低0.001
   200 lsi，降低0.003；topic数待调整
   200 lda，
7. 把百度的qc抓来
